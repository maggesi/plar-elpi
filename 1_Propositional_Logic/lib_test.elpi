%%% ===========================================================================
%%% Tests for lib.
%%% ===========================================================================

accumulate "lib".

%%% ---------------------------------------------------------------------------
%%% Accessory definitions.
%%% ---------------------------------------------------------------------------

pred fun i:int, o:bool.
fun 0 tt.
fun 1 ff.
fun 2 tt.

%%% ---------------------------------------------------------------------------
%%% Run tests.
%%% ---------------------------------------------------------------------------

main :-
    run_tests
    [ report "forallb"
    , okb (forallb [0, 2] fun)
    , kob (forallb [0, 1] fun)
    , report "existsb"
    , okb (existsb [0, 1] fun)
    , kob (existsb [1] fun)
    ].
